import 'package:flutter/material.dart';

//Color

const ktextColor = Colors.black; //bích tiền
const textColor = Colors.black; //thế zjnk
//padding
const double kDefaultPadding = 20.0; // bích tiền
const double defaultPadding = 20.0; // thế zjnk
const primaryColor = Color(0xFFFE724C);
const backgroundColor = Color(0xFFFFF5F5);
const btnColor = Color(0xFFE84C4F);

//lễ
const yellowColor = Color(0xFFFFA317);
const whiteColor = Colors.white;

TextStyle myStyle(double _fontSize, Color _color, FontWeight _fontWeight) {
  return TextStyle(
    fontSize: _fontSize,
    color: _color,
    fontWeight: _fontWeight,
  );
}
